---
name: architecture-solution-writer
description: 架构解决方案编写与评审助手。通过交互式问答收集项目信息，分析用户提供的材料，生成符合"TOC-AAM-T001中国体育彩票架构解决方案模板"格式的完整架构解决方案文档（.docx）；或对用户提供的架构解决方案文档进行评审，检查内容完整性、规范符合性、技术合理性等。当用户需要编写、创建、生成、评审中国体育彩票架构解决方案、架构设计文档、架构方案时触发此skill。适用于体彩技术中心架构部的架构解决方案编写和评审工作流。
---

# 架构解决方案编写与评审助手

本技能支持两种工作模式：
1. **编写模式**：通过交互式问答收集项目信息，结合用户提供的材料，生成符合 TOC-AAM-T001 模板格式的完整架构解决方案文档。
2. **评审模式**：对用户提供的架构解决方案文档进行评审，检查内容完整性、规范符合性、技术合理性等。

## 工作模式判断

首先判断用户意图：
- 如果用户要求"编写"、"创建"、"生成"架构解决方案 → 进入**编写模式**
- 如果用户要求"评审"、"审查"、"检查"、"审核"架构解决方案 → 进入**评审模式**

---

# 编写模式

## 工作流程总览

```
1. 材料收集与读取 → 2. 分阶段信息问答 → 3. 章节适用性判断 → 4. 逐章内容生成 → 5. 文档输出与完整性检查
```

## 第1步：材料收集与读取

1. 检查用户是否上传了材料文件（需求申请单、业务需求说明书、架构规划等）
2. 如有上传文件，使用 Read 工具读取文件内容并提取关键信息
3. 如无上传文件，进入第2步时优先询问用户是否能提供相关材料

## 第2步：分阶段信息问答

使用 AskUserQuestion 工具分阶段向用户提问，每次提问控制在1-4个问题以内。参考 `resources/questionnaire.md` 中的问卷结构，按以下6个阶段进行：

### 阶段一：基础信息
- 项目名称、业务领域、需求类型
- 建设目标与范围
- 输入材料确认
- 术语与缩写

### 阶段二：业务与技术需求
- 功能性需求
- 非功能性需求（RTO/RPO/TPS等）
- 假设和约束

### 阶段三：现状与架构设计
- 现状分析
- 应用架构设计（总体设计、关联系统、核心场景、接口等）
- 技术架构
- 数据架构

### 阶段四：基础设施与安全
- 迁移方案
- 安全架构
- 基础架构
- 成本预估

### 阶段五：运维与附录
- 运维架构
- 附录需求（询问用户是否需要附录A-D可用性分层模型）

### 阶段六：补充确认
- 作者信息
- 版本信息
- 最终确认

**重要原则**：
- 每个阶段结束后，根据已收集信息判断后续阶段是否需要
- 如果用户提供的材料已经包含足够信息，减少重复提问
- 对于用户明确表示"不涉及"的内容，直接跳过相关阶段

## 第3步：章节适用性判断

根据收集到的信息，判断每个章节的适用性。参考 `resources/not-applicable-guide.md`：

- **必须编写**：第1章（系统概述）、第2章（业务需求）、第5章（应用架构）、第9章（安全架构，至少安全定级）、第12章（运维架构，至少运维等级）
- **条件编写**：第3章（假设和约束）、第4章（现状分析）、第6章（技术架构）、第7章（数据架构）、第8章（迁移方案）、第10章（基础架构）
- **条件填写**：第11章（成本预估，无则写"无"）、第13章（附录，无则写"无"）

## 第4步：逐章内容生成

参考 `resources/template-structure.md` 中的完整章节结构和编写说明，逐章生成内容。

### 文档格式要求

1. 使用 docx skill（docx-js）生成 .docx 文件
2. 文档语言：中文
3. 页面大小：A4（11906 x 16838 DXA）
4. 页边距：上下左右 1440 DXA（1英寸）
5. 包含封面页（项目名称 + "架构解决方案" + "技术中心架构部"）
6. 包含文件版本说明表格和作者表格
7. 只使用一个 section，所有内容放在一个 section 的 children 中

### 字体规范（必须严格遵循模板）

**默认字体（Normal正文）**：
- ascii/hAnsi: "Times New Roman", eastAsia: "宋体"
- sz: 24（小四号=12pt）, 行距: 360（单倍行距）

**封面标题**：微软雅黑, 加粗, sz: 72（36pt）, 颜色: 黑色("000000")

**封面副标题**（"架构解决方案"）：宋体, 加粗, sz: 36（18pt）, 颜色: 黑色("000000")

**封面单位**（"技术中心架构部"）：宋体, sz: 28（14pt）, 颜色: 黑色("000000")

**Heading 1（章标题）**：继承Normal字体, 加粗, sz: 44（22pt）, 颜色: 黑色("000000"), 段前340/段后330/行距578

**Heading 2（节标题）**：ascii/hAnsi: "Arial", eastAsia: "宋体", 加粗, sz: 36（18pt）, 颜色: 黑色("000000"), 段前260/段后260/行距413

**Heading 3（子节标题）**：继承Normal字体, 加粗, sz: 32（16pt）, 颜色: 黑色("000000"), 段前260/段后260/行距413

**Heading 4**：ascii/hAnsi: "Arial", eastAsia: "宋体", 加粗, sz: 30（15pt）, 颜色: 黑色("000000"）

**模板斜体说明文字样式**（模板中【...】部分）：宋体, 斜体, 蓝色(#2E75B6), 首行缩进480DXA, sz: 24（小四号）。生成实际内容时替换为真实内容，去掉斜体和蓝色。

**表格字体**：表头-宋体/加粗/sz:21（五号）, 表体-宋体/sz:21（五号）

**docx-js 样式配置**：
```javascript
default: { document: { run: { font: { ascii: "Times New Roman", hAnsi: "Times New Roman", eastAsia: "宋体" }, size: 24 } } }

// Heading1: bold, sz:44, color:"000000", 段前340/段后330/行距578, keepNext:false, keepLines:false
// Heading2: font:{ascii:"Arial",hAnsi:"Arial",eastAsia:"宋体"}, bold, sz:36, color:"000000", 段前260/段后260/行距413
// Heading3: bold, sz:32, color:"000000", 段前260/段后260/行距413
// Heading4: font:{ascii:"Arial",hAnsi:"Arial",eastAsia:"宋体"}, bold, sz:30, color:"000000"
```

### 排版规范

**段落间距**：
- 正文段落：段前0, 段后120（6磅）, 行距360（单倍行距）
- 列表项段落：段前0, 段后60（3磅）, 行距360, 左缩进420（约2字符）, 悬挂缩进420

**编号列表**：
- 使用 LevelFormat.DECIMAL（如 1. 2. 3.），左缩进720, 悬挂缩进360
- 嵌套列表使用 LevelFormat.LOWER_LETTER（如 a. b. c.）

**无序列表**：
- 使用 LevelFormat.BULLET，左缩进720, 悬挂缩进360
- 不使用 unicode bullet 字符（如 •），必须通过 numbering config 实现

**表格规范**：
- 表头背景色：ShadingType.CLEAR, fill: "D5E8F0"
- 表格边框：BorderStyle.SINGLE, size: 1, color: "CCCCCC"
- 单元格边距：top: 80, bottom: 80, left: 120, right: 120
- 表格行：cantSplit: true（防止跨页断裂）
- 表格宽度：必须同时设置 table width 和 cell width，且一致
- 表格水平居中：alignment: AlignmentType.CENTER

**页眉页脚**：
- 页眉：显示文档标题（项目名称 + 架构解决方案），字体：宋体, sz: 18（小五号=9pt）, 颜色: 黑色("000000")
- 页脚：显示页码，居中

### 应用系统清单规范

在编写和评审架构解决方案时，涉及的应用系统必须从《体彩系统清单》中选择，除非是新建设的应用系统。

**《体彩系统清单》结构**：
- 文件位置：`references/体彩系统清单.md`
- 包含125个已建设的应用系统
- 按蓝图位置分为以下组别：
  - 前端应用组
  - 渠道与客户应用组（CF）
  - 发行与运营管理应用组（OF）
  - 合规监管应用组（SF）
  - 数据分析应用组（DF）
  - 业务中台（BP）
  - 数据中台（DP）
  - 游戏（乐透LG/竞猜SG/即开IG/北单BG）
  - 能力开放（PT）
  - 公共技术（PT）
  - 安全（SE）
  - 辅助办公

**系统信息字段**：
- 蓝图位置：系统所属分组
- 系统全称：完整中文名称
- 系统简称：简称
- 英文名称：英文全称
- 技术系统编码（英文简称）：系统编码
- 原中文名称：历史名称
- 系统简介：功能描述

**使用规则**：
1. **编写模式**：
   - 在5.1总体设计中列出涉及系统时，优先从系统清单中选择
   - 使用系统清单中的标准名称（全称或简称）
   - 如涉及新建系统，需明确标注"新建"
   - 如涉及系统变更，需明确标注"变更"

2. **评审模式**：
   - 检查文档中提到的系统名称是否与系统清单一致
   - 检查是否存在未在清单中且未标注"新建"的系统
   - 检查系统简称、编码使用是否规范

3. **命名一致性**：
   - 文档中应使用系统清单中的标准名称
   - 避免使用非标准简称或历史名称（原中文名称）
   - 技术系统编码（英文简称）在涉及接口、数据交换时使用

### 子技能调用

#### 应用系统与架构设计讨论助手

在生成第5章 5.1 总体设计时，如果用户未提供完整的应用架构图或系统交互关系说明，加载 `resources/architecture-design-assistant.md` 子技能进行补充讨论。

**触发时机**：
- 执行到第4步"逐章内容生成"，开始生成第5章时
- 检查是否已有系统清单、数据交换关系、架构图描述
- 如信息不足，暂停主流程，调用子技能进行交互式讨论

**讨论内容**：
1. 识别应用系统（新建/变更/关联），从《体彩系统清单》中选择已有系统
2. 梳理系统间数据交换关系（流向、内容、方式、协议）
3. 确认核心业务流程（1-3个关键流程的系统交互路径）
4. 生成 Mermaid 架构图和流程图代码

**输出整合**：
- 将讨论结果的结构化数据（systems, data_exchanges, core_processes, mermaid_diagrams）整合到5.1总体设计中
- 系统清单以表格形式呈现（包含系统名称、简称、编码、新建/变更/关联标识）
- 数据交换关系以表格或矩阵形式呈现
- 架构图和流程图以图片形式插入（使用 ImageRun），图注格式：宋体, sz: 21, 居中

### 5.1 总体设计 - 应用架构设计图规范

在5.1总体设计章节中，必须包含应用架构设计图，用于直观展示系统整体架构和系统间关系。

#### 架构设计图要求

1. **必须包含的架构图**：
   - **总体应用架构图**：展示所有涉及系统（新建/变更/关联）的层次划分和系统间关系
   - **系统交互关系图**：展示核心系统间的数据交换关系（可选，如关系复杂可单独呈现）

2. **架构图内容规范**：
   - 使用标准系统名称（来自《体彩系统清单》）
   - 标注系统类型：新建（绿色）、变更（黄色）、关联（灰色/蓝色）
   - 展示系统间的数据流向（单向/双向箭头）
   - 按层次划分：前端层、应用层、中台层、数据层、基础设施层等
   - 外部系统（如第三方、监管系统）用虚线框标注

3. **架构图生成方式**：
   - 优先使用 Mermaid 语法生成（graph TD / graph LR）
   - 生成后将 Mermaid 代码渲染为图片插入文档
   - 图注格式："图X-X 总体应用架构图"，宋体, sz: 21, 居中

4. **架构图示例（Mermaid）**：

   ```mermaid
   graph TD
       subgraph 前端层
           A[中国体彩网]
           B[体彩APP]
           C[传统终端 CCPOS]
       end
       
       subgraph 渠道与客户层
           D[终端交易系统 CSF]
           E[购彩者服务系统 CLS]
       end
       
       subgraph 发行与运营管理层
           F[实体渠道管理系统 PCM]
           G[销售服务系统 SOM]
       end
       
       subgraph 业务中台
           H[订单中心 ODC]
           I[支付中心 PMC]
       end
       
       subgraph 数据中台
           J[数据存储与加工中心 DP-DSPC]
       end
       
       A --> D
       B --> D
       C --> D
       D --> E
       D --> H
       H --> I
       F --> G
       G --> J
       I --> J
   ```

#### 5.1 章节内容结构

```
5.1 总体设计
├── 5.1.1 系统清单
│   └── 表格：系统名称、简称、编码、类型（新建/变更/关联）
├── 5.1.2 应用架构设计图
│   ├── 总体应用架构图（Mermaid渲染为图片）
│   └── 图注说明
├── 5.1.3 系统交互关系（可选）
│   └── 数据交换关系表格或矩阵
└── 5.1.4 架构设计说明
    └── 对架构图的补充文字说明
```

### 5.3 核心场景验证 - 流程图/时序图规范

在5.3核心场景验证章节中，必须包含核心业务流程的可视化图表，用于验证架构设计的合理性。

#### 流程图/时序图要求

1. **必须包含的图表**：
   - 至少1-3个核心业务流程的时序图或流程图
   - 每个核心场景对应一个流程图或时序图
   - 优先使用时序图展示系统间交互，使用流程图展示业务步骤

2. **图表内容规范**：
   - 展示参与系统的标准名称（来自《体彩系统清单》）
   - 展示系统间的调用顺序和数据流向
   - 标注关键步骤编号，与文字描述对应
   - 异常处理流程用虚线或不同颜色标注（可选）
   - 涉及外部系统的步骤用虚线框标注

3. **图表类型选择**：
   - **时序图（sequenceDiagram）**：适用于展示多个系统间的交互顺序、请求/响应关系
   - **流程图（flowchart）**：适用于展示业务步骤、判断分支、循环逻辑
   - **状态图（stateDiagram）**：适用于展示状态转换（如订单状态、审批状态）

4. **图表生成方式**：
   - 优先使用 Mermaid 语法生成
   - 生成后将 Mermaid 代码渲染为图片插入文档
   - 图注格式："图X-X {场景名称}流程图/时序图"，宋体, sz: 21, 居中

5. **时序图示例（Mermaid）**：

   ```mermaid
   sequenceDiagram
       participant A as 购彩者
       participant B as 体彩APP
       participant C as 终端交易系统 CSF
       participant D as 订单中心 ODC
       participant E as 支付中心 PMC
       participant F as 游戏交易系统 GTS
       
       A->>B: 1.选择游戏、投注内容
       B->>C: 2.提交投注请求
       C->>D: 3.创建订单
       D->>E: 4.发起支付
       E->>C: 5.支付结果通知
       C->>F: 6.提交投注数据
       F->>C: 7.投注确认
       C->>B: 8.返回投注结果
       B->>A: 9.展示投注成功
   ```

6. **流程图示例（Mermaid）**：

   ```mermaid
   flowchart TD
       A[开始] --> B{用户登录}
       B -->|未登录| C[跳转登录页]
       C --> D[输入账号密码]
       D --> E{验证通过?}
       E -->|否| F[提示错误]
       F --> D
       E -->|是| G[进入首页]
       B -->|已登录| G
       G --> H[选择游戏]
       H --> I[填写投注内容]
       I --> J[确认投注]
       J --> K[支付]
       K --> L{支付成功?}
       L -->|否| M[支付失败处理]
       L -->|是| N[生成彩票]
       N --> O[结束]
   ```

#### 5.3 章节内容结构

```
5.3 核心场景验证
├── 5.3.1 场景一：{场景名称}
│   ├── 场景描述
│   ├── 参与系统
│   ├── 时序图/流程图（Mermaid渲染为图片）
│   ├── 图注说明
│   └── 场景验证结论
├── 5.3.2 场景二：{场景名称}
│   └── ...
└── 5.3.3 场景三：{场景名称}
    └── ...
```

#### 核心场景选择建议

根据项目类型，优先选择以下典型场景：
- **销售类项目**：投注流程、支付流程、兑奖流程
- **渠道类项目**：渠道接入流程、渠道管理流程、渠道结算流程
- **游戏类项目**：游戏发布流程、开奖流程、派奖流程
- **风控类项目**：风险监测流程、预警处理流程、合规审计流程
- **数据类项目**：数据采集流程、数据处理流程、数据服务流程

### 各章节生成规则

**有内容的章节**：根据收集到的信息和材料，按照模板编写说明生成完整内容。

**不涉及的章节**：在章节标题下写"本章不涉及。"

**子节不涉及**：在子节标题下写"本节不涉及。"

**无额外内容的章节**：写"无"

### 数据架构编写规则
- 以业务实体（Entity）为核心描述数据架构
- 列出关键数据实体及其属性字段（不直接给数据库表结构设计）
- 明确主数据归属和数据同步机制

### 接口与表结构描述规范

在架构解决方案文档中，除非用户特殊要求，否则：

1. **不给出具体实体的字段明细**
   - 不列出数据库表的详细字段定义（字段名、类型、长度、约束等）
   - 不给出接口的详细请求/响应参数字段清单

2. **按业务实体的含义和中文名进行描述**
   - 数据库表设计：描述表的业务含义、对应的中文名称、包含的核心业务实体关系即可
   - 接口设计：描述接口的功能、对应的业务操作、输入输出的业务实体中文名即可

3. **示例对比**

   ❌ **不应出现**（过于详细的字段级设计）：
   ```
   表名：t_match_schedule
   字段：
   - id BIGINT 主键
   - match_code VARCHAR(32) 赛程编码
   - match_format VARCHAR(20) 赛制格式
   - prize_level_id BIGINT 奖级ID
   ...
   ```

   ✅ **应该出现**（业务实体级描述）：
   ```
   赛程表（t_match_schedule）：存储彩票赛程信息，包含赛程编码、赛制格式、
   关联奖级、省份信息、净投注金额等核心业务数据。
   关联实体：奖级表、省份表
   ```

   ❌ **不应出现**（详细的接口参数字段）：
   ```yaml
   /api/match-schedules:
     post:
       requestBody:
         properties:
           matchCode: string
           matchFormat: string
           prizeLevelId: integer
           provinceCode: string
           netBetAmount: number
   ```

   ✅ **应该出现**（接口功能和业务实体描述）：
   ```
   POST /api/match-schedules
   功能：创建赛程
   输入：赛程信息（包含赛程编码、赛制、奖级、省份、投注金额等）
   输出：创建的赛程实体信息
   ```

4. **例外情况**
   - 仅当用户明确要求提供详细字段设计时，才给出字段级明细
   - 涉及关键业务规则的核心字段（如状态枚举值），可简要说明其业务含义

### 接口设计与数据库设计命名规范

在进行接口设计和数据库表设计时，必须遵循以下命名规范：

#### 1. 词素表使用规则

- 所有命名必须使用词素表（`references/词素表.md`）中定义的标准词汇
- 中文概念优先使用"名词简称"列的英文缩写/全称
- 词素表中的词汇分为以下三类：
  - **基础名词**：如 CODE（码）、COMPANY（公司）、SESSION（会话）等
  - **业务名词**：如 MATCH_SCHEDULE（赛程）、PRIZE_POOL（奖池）、MATCH_FORMAT（赛制）等
  - **状态/操作名词**：如 SYNCED（已同步）、COMPLETE（完成）、TRANSFER（转移）等

#### 2. 数据库命名规范（下划线命名法 snake_case）

| 对象类型 | 命名规则 | 示例 |
|----------|----------|------|
| 表名 | 模块前缀_业务实体_后缀 | `t_match_schedule`、`t_prize_pool_info` |
| 字段名 | 词素组合，全小写下划线分隔 | `match_code`、`prize_level_id`、`create_time` |
| 索引名 | idx_表名_字段名 | `idx_t_match_schedule_match_code` |
| 约束名 | pk_表名 / fk_表名_字段名 | `pk_t_match_schedule`、`fk_t_match_schedule_province_id` |
| 序列名 | seq_表名 | `seq_t_match_schedule` |
| 视图名 | v_业务描述 | `v_match_schedule_detail` |

**数据库命名示例**：
```sql
-- 表名：赛程表
CREATE TABLE t_match_schedule (
    id              BIGINT PRIMARY KEY COMMENT '主键ID',
    match_code      VARCHAR(32) NOT NULL COMMENT '赛程编码',
    match_format    VARCHAR(20) COMMENT '赛制格式',
    prize_level_id  BIGINT COMMENT '奖级ID',
    province_code   VARCHAR(10) COMMENT '省份编码',
    net_bet_amount  DECIMAL(18,2) COMMENT '净投注金额',
    actual_refund   DECIMAL(18,2) COMMENT '实退金额',
    sync_status     TINYINT DEFAULT 0 COMMENT '同步状态：0-未同步，1-已同步',
    create_time     DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time     DATETIME ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
);

-- 索引
CREATE INDEX idx_t_match_schedule_match_code ON t_match_schedule(match_code);
```

#### 3. 前后端代码变量命名规范（小驼峰命名法 camelCase）

| 对象类型 | 命名规则 | 示例 |
|----------|----------|------|
| 类名 | 大驼峰 PascalCase | `MatchScheduleService`、`PrizePoolController` |
| 变量/属性名 | 小驼峰 camelCase | `matchCode`、`prizeLevelId`、`netBetAmount` |
| 方法名 | 小驼峰 camelCase | `getMatchSchedule()`、`updatePrizePool()` |
| 常量名 | 全大写下划线分隔 | `MAX_PRIZE_LEVEL`、`DEFAULT_SYNC_STATUS` |
| 接口名 | 大驼峰，以 I 开头（可选） | `IMatchScheduleService` 或 `MatchScheduleService` |
| DTO/VO 类名 | 大驼峰，后缀标识类型 | `MatchScheduleDTO`、`PrizePoolVO` |
| 枚举名 | 大驼峰，枚举值全大写 | `SyncStatusEnum.SYNCED`、`PrizeLevelEnum.FIRST` |

**代码命名示例**：
```java
// Java 示例
public class MatchScheduleService {
    private static final int MAX_PRIZE_LEVEL = 10;
    
    public MatchScheduleDTO getMatchScheduleByCode(String matchCode) {
        MatchScheduleEntity entity = matchScheduleDao.findByMatchCode(matchCode);
        Long prizeLevelId = entity.getPrizeLevelId();
        BigDecimal netBetAmount = entity.getNetBetAmount();
        // ...
        return convertToDTO(entity);
    }
    
    public void updateSyncStatus(String matchCode, SyncStatusEnum syncStatus) {
        // ...
    }
}
```

```typescript
// TypeScript/前端示例
interface MatchSchedule {
    id: number;
    matchCode: string;
    matchFormat: string;
    prizeLevelId: number;
    provinceCode: string;
    netBetAmount: number;
    actualRefund: number;
    syncStatus: SyncStatus;
    createTime: string;
}

enum SyncStatus {
    UNSYNCED = 0,
    SYNCED = 1
}

function getMatchScheduleDetail(matchCode: string): Promise<MatchSchedule> {
    return api.get(`/api/match-schedule/${matchCode}`);
}
```

#### 4. 接口设计命名规范

| 对象类型 | 命名规则 | 示例 |
|----------|----------|------|
| URL 路径 | 全小写，中划线分隔 | `/api/match-schedules`、`/api/prize-pools/{prizePoolId}` |
| 请求参数 | 小驼峰 | `matchCode`、`prizeLevelId` |
| 响应字段 | 小驼峰 | `matchCode`、`netBetAmount` |
| Query 参数 | 小驼峰 | `provinceCode`、`startTime` |
| Header 参数 | 大驼峰中划线分隔（标准 HTTP） | `X-Request-Id`、`Content-Type` |

**接口设计示例**：
```yaml
# OpenAPI / Swagger 示例
paths:
  /api/match-schedules:
    get:
      summary: 获取赛程列表
      parameters:
        - name: provinceCode
          in: query
          schema:
            type: string
        - name: matchFormat
          in: query
          schema:
            type: string
      responses:
        200:
          content:
            application/json:
              schema:
                type: object
                properties:
                  matchCode:
                    type: string
                  prizeLevelId:
                    type: integer
                  netBetAmount:
                    type: number
    
    post:
      summary: 创建赛程
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                matchCode:
                  type: string
                matchFormat:
                  type: string
                prizePoolId:
                  type: integer
```

#### 5. 命名转换对照表

| 中文概念 | 词素表简称 | 数据库表/字段 | 代码变量/属性 | URL 路径 |
|----------|-----------|--------------|--------------|----------|
| 赛程 | MATCH_SCHEDULE | match_schedule | matchSchedule | match-schedules |
| 奖池 | PRIZE_POOL | prize_pool | prizePool | prize-pools |
| 奖级 | PRIZE_LEVEL | prize_level | prizeLevel | prize-levels |
| 赛制 | MATCH_FORMAT | match_format | matchFormat | match-formats |
| 省份 | PROVINCE | province | province | provinces |
| 净投注 | NET_BET | net_bet | netBet | net-bets |
| 实退 | ACTUAL_REFUND | actual_refund | actualRefund | actual-refunds |
| 已同步 | SYNCED | sync_status | syncStatus | - |
| 编码 | CODE | code | code | - |
| 公司 | COMPANY | company | company | companies |

#### 6. 规范执行检查清单

生成接口设计或数据库设计时，必须检查：
- [ ] 所有概念是否来自词素表
- [ ] 数据库对象是否使用下划线命名法（snake_case）
- [ ] 代码变量是否使用小驼峰命名法（camelCase）
- [ ] 类名是否使用大驼峰命名法（PascalCase）
- [ ] URL 路径是否使用全小写中划线分隔
- [ ] 常量是否使用全大写下划线分隔
- [ ] 同一概念在不同层命名是否保持一致（仅大小写和分隔符不同）

### 附录处理
- 13.1 基础架构容灾等级定义：直接引用 `resources/appendix-reference.md` 中的内容
- 附录A-D（可用性分层模型）：询问用户是否需要，如需要则引用 `resources/appendix-reference.md` 中对应内容

## 第5步：文档输出与完整性检查

1. 使用 docx-js 生成完整的 .docx 文件
2. 文件命名规则：`{项目名称}架构解决方案V{版本号}.docx`
3. 保存到 `/workspace` 目录
4. 运行 sanitize.py 清理文档
5. **自动化完整性检查**（必须执行）：
   - 使用 pandoc 将生成的 .docx 转换为 .md
   - 逐项检查以下关键词是否全部存在：

| 检查项 | 关键词 | 必须存在 |
|--------|--------|----------|
| 封面 | 架构解决方案 | 是 |
| 版本说明 | 版本 | 是 |
| 作者表 | 应用架构 | 是 |
| 第1章 | 系统概述 | 是 |
| 1.1 | 系统建设目标 | 是 |
| 1.2 | 系统建设范围 | 是 |
| 1.3 | 缩写及术语 | 是 |
| 1.4 | 参考文档 | 是 |
| 第2章 | 业务需求 | 是 |
| 2.1 | 功能性需求 | 是 |
| 2.2 | 非功能性需求 | 是 |
| 第3章 | 假设和约束 | 是 |
| 第4章 | 现状分析 | 是 |
| 4.1 | 总体介绍 | 是 |
| 4.2 | 差距分析 | 是 |
| 第5章 | 应用架构 | 是 |
| 5.1 | 总体设计 | 是 |
| 5.2 | 关联系统设计 | 是 |
| 5.3 | 核心场景验证 | 是 |
| 5.4 | 系统接口 | 是 |
| 5.5 | 接口兼容性设计 | 是 |
| 5.6 | 非功能性需求设计 | 是 |
| 5.7 | 关联系统变更实施策略 | 是 |
| 第6章 | 技术架构 | 是 |
| 第7章 | 数据架构 | 是 |
| 7.1 | 结构化数据架构 | 是 |
| 7.2 | 非结构化数据架构 | 是 |
| 第8章 | 迁移方案 | 是 |
| 第9章 | 安全架构 | 是 |
| 9.1 | 安全定级 | 是 |
| 9.2 | 威胁分析 | 是 |
| 9.3 | 网络安全 | 是 |
| 9.4 | 应用安全 | 是 |
| 9.5 | 数据安全 | 是 |
| 9.6 | 主机安全 | 是 |
| 9.7 | 中间件安全 | 是 |
| 第10章 | 基础架构 | 是 |
| 第11章 | 成本预估 | 是 |
| 第12章 | 运维架构 | 是 |
| 12.1.1 | 运维等级 | 是 |
| 12.1.2 | 高可用 | 是 |
| 12.1.3 | 快速恢复 | 是 |
| 12.2.1 | 监控 | 是 |
| 12.2.2 | 工具 | 是 |
| 12.2.3 | 部署 | 是 |
| 12.2.4 | 保障 | 是 |
| 第13章 | 附录 | 是 |
| 13.1.1 | 分级模型 | 是 |
| 13.1.2 | RTO/RPO | 是 |
| 13.1.3 | 关键实施考量 | 是 |

   - 如有缺失，补充缺失内容后重新生成
6. 向用户提供文件链接

---

# 评审模式

## 评审工作流程

```
1. 材料收集与读取 → 2. 评审维度确认 → 3. 逐章评审检查 → 4. 评审报告生成
```

## 第1步：材料收集与读取

1. 检查用户是否上传了待评审的架构解决方案文档
2. 如有上传文件，使用 Read 工具读取文件内容
3. 询问用户评审的重点关注点（可选：完整性、规范性、技术合理性、安全性等）
4. 如用户未提供具体关注点，按以下默认维度进行评审

## 第2步：评审维度确认

默认评审维度：

### 1. 内容完整性检查
- 检查是否包含全部必须章节（第1、2、5、9、12章）
- 检查各章节是否有实质性内容（非"本章不涉及"或"无"）
- 检查关键信息是否缺失（如系统建设目标、业务需求、接口设计等）

### 2. 规范符合性检查
- 检查文档格式是否符合 TOC-AAM-T001 模板要求
- 检查字体、段落、表格等排版规范
- 检查章节编号和层级结构是否正确
- 检查接口和数据库设计是否遵循命名规范（参考"接口设计与数据库设计命名规范"）

### 3. 技术合理性检查
- 检查应用架构设计是否合理（系统划分、交互关系）
- 检查技术选型是否适当
- 检查数据架构设计是否合理（实体关系、主数据归属）
- 检查安全架构是否完整（安全定级、威胁分析、防护措施）
- 检查非功能性需求设计是否满足要求（RTO/RPO/TPS等）

### 4. 一致性检查
- 检查术语使用是否一致
- 检查前后文描述是否矛盾
- 检查图表与文字描述是否一致
- 检查接口定义与数据架构是否一致

### 5. 可实施性检查
- 检查迁移方案是否可行
- 检查成本预估是否合理
- 检查运维架构是否完整

## 第3步：逐章评审检查

参考 `resources/template-structure.md` 中的章节结构，逐章进行评审：

### 评审输出格式

对每个章节输出：
- **章节状态**：✅ 通过 / ⚠️ 建议优化 / ❌ 存在问题
- **问题列表**：按严重程度排序（严重/一般/建议）
- **改进建议**：具体的修改建议

### 严重程度定义
- **严重**：缺少必须内容、存在技术错误、与规范严重不符
- **一般**：内容不完整、描述不清晰、存在小错误
- **建议**：可以优化的地方、补充建议

## 第4步：评审报告生成

生成完整的评审报告，按照以下格式整理评审问题：

### 评审问题记录表格式

评审结果以表格形式输出，包含以下字段：

| 字段 | 说明 | 填写规则 |
|------|------|----------|
| **No.** | 序号 | 从1开始递增 |
| **评审文档名称** | 被评审的文档名称 | 填写文档完整名称，如"{项目名称}架构解决方案V{版本号}" |
| **问题&建议描述** | 评审发现的问题或建议 | 清晰描述问题内容，包括问题位置、具体描述、改进建议 |
| **提出人** | 评审人 | 默认为"AI评审助手"，用户可修改 |
| **提出日期** | 评审日期 | 当前日期 |
| **对应时限** | 解决时限要求 | 根据严重程度填写（严重：3天内；一般：1周内；建议：下次迭代） |
| **负责人** | 问题解决负责人 | 由用户指定或留空 |
| **解决日期** | 问题解决日期 | 留空，待后续填写 |
| **问题对应措施** | 解决措施 | 留空，待后续填写 |
| **结果** | 处理结果 | 可选：完全采纳/部分采纳/无需调整/待讨论 |
| **状态** | 当前状态 | 可选：待处理/处理中/已解决/已关闭 |
| **备注** | 补充说明 | 可选填写 |

### 问题&建议描述格式

每个问题按以下结构描述：

```
【章节】第X章 {章节名} / {子节名}
【严重程度】严重/一般/建议
【问题描述】{具体描述问题内容}
【改进建议】{具体的修改建议}
【参考依据】{相关规范或标准}
```

### 评审报告输出示例

```markdown
# 架构解决方案评审报告

## 一、总体评价

- **文档名称**：{项目名称}架构解决方案
- **评审日期**：{日期}
- **总体评分**：{优秀/良好/合格/不合格}
- **主要结论**：{总体评价摘要}

## 二、评审问题记录表

| No. | 评审文档名称 | 问题&建议描述 | 提出人 | 提出日期 | 对应时限 | 负责人 | 解决日期 | 问题对应措施 | 结果 | 状态 | 备注 |
|-----|-------------|--------------|--------|----------|----------|--------|----------|-------------|------|------|------|
| 1 | {文档名称} | 【章节】第5章 应用架构/5.4 系统接口<br>【严重程度】严重<br>【问题描述】接口设计缺少与实体渠道管理系统的数据交换说明<br>【改进建议】补充双录系统与实体渠道管理系统之间的接口定义，包括巡检数据同步接口<br>【参考依据】TOC-AAM-T001模板5.4节要求 | AI评审助手 | {日期} | 3天内 | | | | | 待处理 | |
| 2 | {文档名称} | 【章节】第7章 数据架构/7.1 结构化数据架构<br>【严重程度】一般<br>【问题描述】视频文件保留期限未明确说明<br>【改进建议】明确视频文件保留期限为24小时，并说明清理机制<br>【参考依据】业务需求说明书 | AI评审助手 | {日期} | 1周内 | | | | | 待处理 | |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

## 三、问题统计

- 严重问题：{数量} 个
- 一般问题：{数量} 个
- 建议优化：{数量} 个

## 四、优先改进项

1. [严重] {问题描述} → {改进建议}
2. ...
```

### 生成要求

1. **评审文档名称**必须填写完整文档名称（包含版本号）
2. **问题&建议描述**必须包含：章节位置、严重程度、问题描述、改进建议、参考依据
3. 每个问题单独一行，使用 `<br>` 或换行分隔描述中的不同部分
4. 问题按严重程度排序（严重→一般→建议），同严重程度按章节顺序排列
5. 结果、状态、负责人、解决日期、问题对应措施可先留空，由用户后续填写

## 参考文件

- `resources/template-structure.md`：完整的模板章节结构及各章节编写说明
- `resources/questionnaire.md`：分阶段信息收集问卷
- `resources/not-applicable-guide.md`：各章节"不涉及"判断指南
- `resources/appendix-reference.md`：附录标准参考数据（容灾等级定义、RTO/RPO指标矩阵、可用性分层模型等）
- `resources/architecture-design-assistant.md`：应用系统与架构设计讨论助手（子技能），用于5.1总体设计前梳理系统清单、数据交换关系和生成架构/流程图
